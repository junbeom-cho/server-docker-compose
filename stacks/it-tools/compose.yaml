services:
  it-tools:
    image: corentinth/it-tools:latest
    ports:
      - 8081:80
    restart: unless-stopped
    container_name: it-tools
    networks:
      - npm_default
networks:
  npm_default:
    external: true
