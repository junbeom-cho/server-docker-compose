services:
  app:
    # 1. CrowdSec 바운서가 내장된 이미지로 교체
    image: lepresidente/nginxproxymanager:latest
    restart: unless-stopped
    ports:
      - 80:80
      - 443:443
      - 81:81
    environment:
      TZ: Asia/Seoul
      # 기존 DB 설정 그대로 유지 (중요!)
      DB_MYSQL_HOST: db
      DB_MYSQL_PORT: 3306
      DB_MYSQL_USER: npm
      DB_MYSQL_PASSWORD: ${NPM_DB_PASSWORD}
      DB_MYSQL_NAME: npm
      # CrowdSec 연동 설정 추가
      CROWDSEC_OPENRESTY_BOUNCER: |
        ENABLED=true
        API_URL=http://crowdsec:8080
        API_KEY=${CROWDSEC_BOUNCER_APIKEY}
        CACHE_EXPIRATION=1s
    volumes:
      - /opt/docker/npm/data:/data
      - /opt/docker/npm/letsencrypt:/etc/letsencrypt
    networks:
      - npm_default
      - crowdsec_internal # CrowdSec과 대화할 전용선
    depends_on:
      - db
      - crowdsec

  db:
    # 기존 DB 설정 100% 동일하게 유지
    image: jc21/mariadb-aria:latest
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: ${NPM_DB_ROOT_PASSWORD}
      MYSQL_DATABASE: npm
      MYSQL_USER: npm
      MYSQL_PASSWORD: ${NPM_DB_PASSWORD}
      MARIADB_AUTO_UPGRADE: "1"
    volumes:
      - /opt/docker/npm/mysql:/var/lib/mysql
    networks:
      - npm_default

  crowdsec:
    image: crowdsecurity/crowdsec:latest
    container_name: crowdsec
    restart: unless-stopped
    environment:
      # NPM 전용 컬렉션 자동 설치
      COLLECTIONS: "crowdsecurity/nginx-proxy-manager crowdsecurity/base-os"
      GID: "${GID-1000}"
    volumes:
      - /opt/docker/crowdsec/acquis.yaml:/etc/crowdsec/acquis.yaml
      - /opt/docker/crowdsec/config:/etc/crowdsec
      - /opt/docker/crowdsec/data:/var/lib/crowdsec/data
      # [중요] 에러 방지를 위해 호스트의 NPM 로그를 '/npm_logs'라는 별도 경로로 마운트
      - /opt/docker/npm/data/logs:/npm_logs:ro
    networks:
      - crowdsec_internal

networks:
  npm_default:
    external: true
  crowdsec_internal:
    driver: bridge