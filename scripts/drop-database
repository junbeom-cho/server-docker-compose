-- 1. 데이터베이스 연결 강제 종료 (필요 시)
SELECT pg_terminate_backend(pg_stat_activity.pid)
FROM pg_stat_activity
WHERE pg_stat_activity.datname = 'dockhand_db'
  AND pid <> pg_backend_pid();

-- 2. 데이터베이스 삭제
DROP DATABASE IF EXISTS dockhand_db;

-- 3. 사용자 삭제
DROP USER IF EXISTS dockhand_user;
